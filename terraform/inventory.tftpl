%{ for i, host in mon_vm_names ~}
${mon_vm_names[i]} ansible_host=${mon_ip_address_list[i]}
%{ endfor ~}
%{ for i, host in mds_vm_names ~}
${mds_vm_names[i]} ansible_host=${mds_ip_address_list[i]}
%{ endfor ~}
%{ for i, host in osd_vm_names ~}
${osd_vm_names[i]} ansible_host=${osd_ip_address_list[i]}
%{ endfor ~}

[monitoring]
${mon_vm_names[0]}

[mons]
%{ for i, host in mon_vm_names ~}
${mon_vm_names[i]}
%{ endfor ~}

[mgrs]
%{ for i, host in mon_vm_names ~}
${mon_vm_names[i]}
%{ endfor ~}

[mdss]
%{ for i, host in mds_vm_names ~}
${mds_vm_names[i]}
%{ endfor ~}

[osds]
%{ for i, host in osd_vm_names ~}
${osd_vm_names[i]}
%{ endfor ~}

[all:vars]
public_ip=${public_ip_address}
ansible_user=ubuntu
